
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="format-detection" content="telephone=no"/>
    <meta name="format-detection" content="email=no"/>
    <meta content="width=device-width, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, user-scalable=0" name="viewport">
    <title>邓德元</title>
    <style>
      *{margin:0;padding:0;box-sizing: border-box;}
      *, *:active, *:focus{outline: 0 none;}
      html body{height: 100%;width: 100%;}
      body{font-size:12px;}
      .app{position: absolute;width:100%;height: 100%;background: url('https://dn-caicloudui.qbox.me/ddy/1.jpg') top no-repeat;background-size: 100%;}
      .btn{position: absolute;top:60px;right:50px;background: red;width:100px;height:100px;transform: rotate(45deg);}
      .btn:before, .btn:after{position: absolute;width:100px;height:100px;border-radius: 50%;background: red;content: '';}
      .btn:before{left: -50px;}
      .btn:after{top:-50px;}
      .btn .text{color:#fff;font-size:20px;position: absolute;left:0;top:0;}
      .input{position: absolute;top:120px;left:30px;width:160px;height:40px;}
      .input input{border:none;width:100%;height: 100%;line-height: 40px;font-size:14px;padding:0 5px;border: red 2px solid}
    </style>
  </head>

  <body>
    <div class="app"></div>
    <div class="input">
      <input type="text" />
    </div>
    <div class="btn">
      <div class="text">111</div>
    </div>
    <div id="app"></div>
    <button id="submit"></button>
  </body>

<script src="/js/zepto.min.js"></script>
<script src="/js/socket.io.js"></script>
<script>
(function(){
  var doc = document, body = doc.body;

  var chat = {
    data: null,

    init: function() {
      var w = this;
      var socket = io.connect('http://localhost:8080');
      w.socket = socket;

      w.initEvent();
    },

    initEvent: function(){
      var w = this;

      w.socket.on('init', function(d){
        $('#submit').text(d.text);
        w.data = d;
      });

      $('#submit').click(function(){
        w.socket.emit('submit', w.data);
      })
    }
  }

  chat.init();
})();

</script>

</html>
